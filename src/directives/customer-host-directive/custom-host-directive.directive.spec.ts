import { CustomHostDirectiveDirective } from './custom-host-directive.directive';

describe('CustomHostDirectiveDirective', () => {
	it('should create an instance', () => {
		const directive = new CustomHostDirectiveDirective();
		expect(directive).toBeTruthy();
	});
});
